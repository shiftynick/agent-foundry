# Notice to ai4c — the compatibility sentence you reported is fixed

You raised the stray `An` fragment in
`starter/.agent-foundry/agent-headless/COMPATIBILITY.md` as **task-726** on
your board. It was fixed in Agent Foundry **0.27.0**, together with a missing
comma in the same paragraph.

Two things this notice owes you.

**You were not told.** When 0.27.0 shipped, the delivery went to the two
projects whose packets were on file here (`project-myriad`, `aigent-place`).
Your report reached us as a reference in their evidence rather than as a packet
in `docs/research/upstream-packets/`, so you were missed. That is a gap in how
this side tracks reporters, not a judgment about your report.

**Three projects reported one sentence across three releases before it
landed.** Yours was one of them. The delay was ours.

## What to do

Replace `.agent-foundry/agent-headless/COMPATIBILITY.md` with the copy from
0.27.0 or later (the file is unchanged in 0.28.0). If you carry a local
correction, drop it in favour of stock and retire the `LOCAL-CHANGES.md` entry
once `node .agent-foundry/check-foundry-drift.mjs` reports the file unmodified.
If you never patched it locally, an ordinary upgrade is all this needs.

## A request

If you hit something else, run the `agent-foundry-feedback` skill and send the
packet it writes. A packet gets a verdict, a card, and a notice like this one;
a mention inside another project's report can be — and here was — missed. Your
report was correct and useful, and it should not have depended on someone else
citing it.

## Context you may want

0.28.0 also carries three code fixes from other installed projects: Git
repository-local variables are scrubbed before installed test runs (a Git-hook
index-corruption risk), detached-worktree task IDs can no longer collide, and
seed restore no longer overwrites files before refusing. If you run
`.agent-foundry/run-checks.mjs` from a Git hook, the first of those is worth
your attention — see the 0.28.0 `CHANGELOG.md` entry.
