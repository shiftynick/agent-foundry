# Nightly audit — 2026-08-08

First run; no baseline — every number below becomes tomorrow's comparison.
Method: deterministic day/repo selection (session-audit `discover.mjs` for
Claude; the Codex date tree with cwd matching), grep-built marker indexes
(11 files, ~24 MB raw → 376 KB indexed), three parallel read-only
extractors, main-context synthesis. Extraction JSON stays in the run
directory outside Git; every claim below carries a resolvable citation.

Coverage notes: N:\aigent-place was not yet in the repo set when extraction
ran (2 Codex sessions, ~3.4 MB — tomorrow). One interra session and one
myriad session started before the audit day; their day-of work was counted
from task logs, transcripts not indexed (flagged per skill rule). Synoptic
crossed the threshold gate (two sessions, ~35 and ~30 min) and was audited.

## Dashboard

| Metric | ai4c | project-myriad | interra-api-proxy | synoptic |
| --- | --- | --- | --- | --- |
| Sessions / active time | 5 (2 codex, 1 claude + 2 subagents) / ~2.5 h active | 4 (2 codex, 2 claude*) / ~10 h | 2 (1 codex, 1 prior-day*) / ~1.5 h | 2 codex / ~65 min |
| Work units completed | 8 tasks done | 10 tasks done (+1 filed) | 1 done + 2 progressed; PRs #152, #153 merged | 3 done (bootstrap + upgrade + closeout) |
| Review rounds / completed task | 19/8 ≈ 2.4 | 22/10 ≈ 2.2 | 4/1 (+3 delta PASSes) | 8/3 ≈ 2.7 |
| Preventable-class occurrences | 13 (11 evidence-gap, 2 packet-defect) | 8 (4 evidence-gap, 3 packet-defect, 1 fix-defect) | ~4 (packet/evidence ≈1 round; 1 fix-defect; 1 overclaim) | ~5 (evidence-gap dominant) |
| Waste events / rough time | 4 / ~10 min | 6 / ~1 review round + minutes | 3 / ~20 min waits + 1 round | 2 / ~10 min waits |

*myriad's morning session (tasks 054/057) and interra's 096/097 session
predate the day's transcript window; counted from dated log entries.

Payload versions in play: 0.18.0 → 0.23.0 → 0.24.0 (all four repos upgraded
during this very day; none yet runs 0.25.0, whose rules target the
preventable classes above).

- **Biggest waste — ai4c:** upgrade round 1 spent 4 findings on evidence
  mechanics at once (gates run but not durably recorded)
  (`N:\ai4c\.tasks\archive\task-2561858008000001-upgrade-agent-foundry-0-23-0-0-24-0.md:109`),
  plus one malformed STANDARDS output forcing a full axis rerun (same line).
- **Biggest waste — myriad:** Windows argument forwarding truncated task
  notes, and under the new severity gate that single record defect cost a
  full extra two-axis round
  (`git acfe88b:.tasks/tasks/task-9759177867000001-…md:295`); PowerShell
  one-liner quoting failed three separate times before Node helpers were
  adopted (`task-054…md:25-29`, `:309`).
- **Biggest waste — interra:** a round-1 fix exempted exactly the traffic
  ADR 0006 exists to bound — `fixesReal=FALSE`, one full round to undo
  (`N:\interra-api-proxy\.tasks\tasks\task-096-…md:25`).
- **Biggest waste — synoptic:** review provenance lost by invoking rounds
  1–2 outside `task.mjs run --json`; round 3 partly existed to repair the
  record (`N:\synoptic\.tasks\tasks\task-001-…md:340`).
- **Best practice — ai4c:** cross-family review in *both directions*
  (codex-implemented/claude-reviewed and the reverse) with 2–6 min round
  turnaround; tightly scoped follow-up cards closed in one round — task-734
  in 15 min, task-735 in 11 min (`task-734…md:89`, `task-735…md:64`).
- **Best practice — myriad:** the 0.24.0 severity gate fired correctly on
  its first day (medium defect → forced round 3 → closed clean), and a
  procedurally-correct reviewer finding was resolved by *recorded rubric
  amendment* instead of silent reinterpretation (`task-049…md:254`).
- **Best practice — interra:** upgrade executed in an isolated worktree
  while a parallel session advanced main; clean rebase, both PRs merged
  same evening, zero conflicts; false evidence claim retracted and replaced
  with a verified run under cap pressure (`task-3385…md:254,358`).
- **Best practice — synoptic:** operator gates honored mid-flow (milestone
  approval pause; nothing pushed without authority) on a repo bootstrapped
  that same afternoon (`task-001…md:165-166`).

## Session notes (condensed)

- **ai4c / claude-fable-5** (parent, ~8.5 h span with an evening gap):
  homepage tasks 722/723/724 through review, staging deploy, prod
  verification; dispatched two opus subagents (735: one round, 11 min;
  730: 3 rounds, 33 min, mutation-verified fixes). Reviewer: codex-cli.
  A mandated visual-evidence pass caught a real defect reviewers missed
  (`task-722…md:117`).
- **ai4c / codex gpt-5.6-sol** (2 sessions, ~62 min): both Foundry
  upgrades; one env-setup miss (premerge in fresh worktree without
  node_modules, ~5 min), one baseline-pollution finding (pre-existing lint
  red spun off as task-734 and fixed same day).
- **myriad / codex gpt-5** (2 sessions, ~41 min): both upgrades; worktree
  isolation; the truncation incident above.
- **myriad / claude opus-5→fable-5** (attack-the-board, ~9 h): six-task
  chain 050→033→049→035→036→037, all through cold review, 5/10 day's tasks
  hit the 3-round cap; heavy background-subagent use with no idle polling
  observed; recurring link/path-confinement misses across three tasks
  (`task-035…md:291`) — the same lens keeps firing at review time instead
  of write time.
- **interra / codex gpt-5.6-sol** (~44 min + evening segment): upgrade in
  isolated worktree; ~10 min of review-wait heartbeats with nothing
  overlapped; ~9 min CI waits (inherent); 12 stale remote branches cleaned.
- **synoptic / codex gpt-5.6-sol + gpt-5.6-terra** (~65 min): Foundry
  bootstrapped 0.23.0 and upgraded 0.24.0 the same day; found a genuine
  installer edge case (no remote HEAD → defaultBranch recorded from the
  active branch) and logged it as upstream-worthy (`task-015…md:69`).

## Findings by class (day totals; batches floored where noted)

Entries: ai4c 51 (12 clean), myriad 36 recorded / ~62 exploded (5 clean),
interra 19 (3 clean), synoptic 11 (2 clean). Dominant classes today:
`evidence-gap` (~20 across all four repos), `doc-contradiction` (~15),
`weak-validation` (~12, mostly myriad's factory/validation work — review
operating as intended), `clean-round` (22). `vacuous-oracle` appeared 4
times (ai4c probe, myriad binding checks) — all caught and
mutation-verified. `fix-defect` twice (interra 096, myriad 033 —
claimed-but-unapplied). `relitigation` 5 (myriad 4, ai4c 1).

## Candidates

1. **ADOPT (payload)** — recorded evidence commands keep failing on Windows
   shell metacharacters (3 myriad + 1 synoptic + 3 interra occurrences, one
   costing a full review round): add one rule at the point of use in
   `execute-task` → Validate: when a recorded command needs quoting or
   metacharacters, write a script file and run that, instead of an inline
   one-liner. Filed as a task on this board.
2. **ADOPT (repo practice)** — all four repos ended the day on 0.24.0;
   `evidence-gap`/`packet-defect` dominate their preventable spend and the
   0.25.0 rules target exactly those. Suggestion: upgrade all four to
   0.25.0 before the next working day; tomorrow's dashboard then measures
   the rules' effect directly.
3. **REVISE (existing work)** — blocked review waits with nothing
   overlapped (~20 min interra, ~10 min synoptic) are the pattern task-022
   already owns; today's citations add evidence, no new mechanism.
4. **WATCH (operator habit)** — 5 of 10 myriad tasks hit the 3-round cap
   during a single attack-the-board chain; may indicate multi-slice tasks
   sized past one context. One day is not a pattern; watch.
5. **WATCH (payload)** — review provenance loss when axes are invoked
   outside `task.mjs run --json` (synoptic); the skill text already
   prescribes recording — if it recurs, sharpen the wording rather than add
   a rule.
6. **STOP** — the `weak-validation`/`security-boundary` volume in myriad's
   factory work is adversarial review doing its job on genuinely hard
   surfaces; no prevention proposal.
