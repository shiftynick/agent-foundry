# Nightly audit — 2026-08-09

Morning-of run on 2026-08-10: covers operator-local Sunday 2026-08-09
through overnight into ~08:37 EDT Monday (window
`2026-08-09T04:00:00Z` → `2026-08-10T12:37:00Z`). Method: Claude
`discover.mjs` for day-started parents; Codex date-tree copies (share-locked
originals) with cwd matching; marker-indexed transcripts; three parallel
read-only extractors; main-context synthesis. Extraction JSON stays under
`session-audit-out/2026-08-09.session-audit-run/` (gitignored).

Coverage notes: **aigent-place** crossed the threshold gate hard (three
Codex sessions, ~8.8 h active) and gets full depth — first inclusion after
being absent from 2026-08-08. **synoptic** stayed below gate (upgrade-only
~2.5 min). Two large Codex rollouts under
`C:\Users\shift\Documents\Codex\2026-08-09\i-w` are **Hermes/OVH personal
work**, not interra — excluded. No authentic in-window interra agent
transcript; only the ~2 min 0.28 upgrade card. Baseline: [2026-08-08](./2026-08-08.md).

## Dashboard

| Metric | ai4c | project-myriad | aigent-place | interra-api-proxy | synoptic |
| --- | --- | --- | --- | --- | --- |
| Sessions / active time | 1 Claude (+1 subagent) / ~2.6 h active | 1 Codex (+ nested Cursor/Opus) / ~5.5 h | 3 Codex / ~8.8 h | 0 agent sessions; ~2 min upgrade | below gate (~2.5 min) |
| Work units completed | 4 done (+3 progressed) | 4 done (+1 blocked at 3-round cap) | 7 done (+1 in_progress dead-end) | 1 done (upgrade) | 1 done (upgrade) |
| Review rounds / completed task | 7/4 ≈ 1.8 (9 incl. in-flight) | ~11/4 ≈ 2.8 | 17/7 ≈ 2.4 | 0 | 0 |
| Preventable-class occurrences | 8 (6 evidence-gap, 2 packet-defect) | ≥7 (evidence-gap dominant + 1 fix-defect + packet) | ~5 (3 evidence-gap, 2 packet-defect) | 0 | 0 |
| Waste events / rough time | 6 / ~167 min (dominated by 140 min issue-build wait) | ~8 / ~45–90 min (serial review waits + Cursor shell-deny) | ~12 / ~135 min (Cursor/900s ceilings + 052 dead-end) | 0 | n/a |

Deltas vs 2026-08-08: preventable counts stayed high **after** every audited
repo reached **0.28.0** mid-window (Aug 8 ended on 0.24.0; the 0.25.0+ rules
targeting these classes are now live and still not holding on first pass).
ai4c review rounds/task improved (2.4 → 1.8). aigent-place is new on the
dashboard. interra/synoptic collapsed to upgrade-only vs yesterday's product
work.

Payload versions: all five repos **0.28.0** by end of window.

- **Biggest waste — ai4c:** ~140 min idle session gap waiting on the Sunday
  issue build with little overlapped work
  (`749dc7c0…jsonl` ~L160→L200; `N:\ai4c\.tasks\tasks\task-732-….md:21`).
- **Biggest waste — myriad:** serial high-effort Opus dual-axis waits with
  dense status pings and little overlapped work, plus Cursor shell-deny
  forcing orchestrator re-integration on task-048
  (Codex copy `…T19-01-38…jsonl` e.g. `:2424`–`:2476`, `:3562`).
- **Biggest waste — aigent-place:** Cursor provider ~20 m vs
  `task.mjs run` **900 s** ceiling mismatch across 048/049/051, plus a ~30
  min task-052 dead-end that imported nothing
  (`task-048…md:56`, `task-049…md:31`, `task-052…md:25`).
- **Best practice — ai4c:** live W33 measurements → dependency-ordered
  pool-depth front → attack-the-board with parallel Codex reviews; schedule-
  gated cards left in `review` instead of closed on inference
  (`task-741…md:160`, `task-732…md:526`).
- **Best practice — myriad:** Codex orchestrates isolated Cursor/Grok
  worktrees + opposite-family Opus reviews; rejects bad worker patches
  before land; honest rubric narrowing when overclaim found
  (`task-059…md:98`; Codex `:1504`).
- **Best practice — aigent-place:** cold review earned keep on task-051
  (HIGH active-collider-set caught before merge; five rounds closed real
  motion bugs) (`task-051…md:143,233,267`).

## Session notes (condensed)

- **ai4c / claude opus-5→fable-5** (~2.6 h active across evening + overnight +
  morning): deploy prior cleanups; foundry 0.27→0.28 (task-740); wait through
  Sunday issue build; measure W33 pool decay; file and attack pool-depth
  front (732/741–745); 732 three rounds (packet-defect forced R3); morning
  founder-reported email click→localhost (747 to review at window end).
  Reviewer: Codex via agent-headless.
- **myriad / codex gpt-5.6-terra** (~5.5 h active overnight): attack-the-board
  with Cursor Grok 4.5 High implement + Claude Opus review — 064 upgrade,
  046 blocked at 3-round cap, 059/034/048 done. Recurring junction/path-
  confinement and evidence-gap themes from Aug 8 continue under 0.28.0.
- **aigent-place / codex gpt-5.6-sol + terra** (~8.8 h): midday push recovery;
  evening task-060 upgrade reopen after premature `done`; overnight collision-
  core chain 048→aggregate→049→050→051 plus 039; 052 incomplete. Cursor
  timeouts dominate waste; Fable cold reviews caught real HIGH defects.
- **interra:** no in-window product session. Upgrade card only. Do not
  attribute the Hermes/OVH `i-w` Codex threads.
- **synoptic:** below gate; 0.28 upgrade only (~2.5 min).

## Findings by class (day totals; small-day batches exploded)

Dominant across focus+threshold material: `evidence-gap` (~15+),
`clean-round` (~38 axes), `missing-test` / `missing-semantics` (product
review doing its job, especially aigent 051 and ai4c 732),
`packet-defect` (~5), `review-harness-failure` (~4 on aigent — wrong model
id, inherit-session, truncated STANDARDS). `fix-defect` once (myriad 059
Assert.Skip). `relitigation` rare (ai4c 743, rejected via recorded decision).

Preventable trio under **0.28.0** still non-zero on the day the mold landed:
ai4c 8, myriad ≥7, aigent ~5. Named rules that did not hold on first pass:
packet completeness, durable gate/review evidence before `done`, recorded
final-main evidence.

## Candidates

1. **ADOPT (payload)** — align `task.mjs run` / agent-headless timeout
   ceilings with Cursor's ~20 m provider budget (or warn and refuse a 900 s
   wrap around a Cursor coding run). aigent-place alone lost ~80–100 min to
   the mismatch plus a 052 dead-end under the same ceiling
   (`task-048…md:56`, `task-049…md:31`, `task-052…md:25`). Filed on this
   board.
2. **REVISE (existing work)** — blocked review waits with nothing overlapped
   recur (myriad serial Opus waits; aigent Cursor waits). task-022 already
   owns the sanctioned-wait pattern; today's citations add evidence, no new
   mechanism. Annotated on task-022.
3. **WATCH (payload)** — preventable classes remain high on day-one of
   0.28.0 across three repos (second consecutive audit day with the same
   trio dominant). One more recurrence makes retrospective-grade evidence
   for sharpening the 0.25.0+ rules rather than adding parallel guidance.
4. **WATCH (operator habit)** — myriad again hit the 3-round cap (046
   blocked; 048 accepted at cap), continuing the Aug 8 attack-the-board
   pattern. Still two days, not three.
5. **STOP** — aigent 051's `missing-semantics` / HIGH collider findings and
   ai4c's `missing-test` volume are adversarial review working; no prevention
   proposal.
6. **No change earned** for interra/synoptic thin upgrade-only days, beyond
   the audit method note: match Codex sessions by repo cwd, not by date-tree
   adjacency.
